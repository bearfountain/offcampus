- properties.each_with_index do |property, index|
  - if index % 3 == 0
    .row
  %a{:href => "/properties/" + property.id.to_s}
    .post
      - if user_signed_in? and property.landlord_id == @user.landlord.id
        = link_to 'edit info', edit_property_url(property), :class => 'edit'
      - if property.has_photo?
        = image_tag property.featured_photo.photo.url(:thumb), :class => 'house'
      .info
        %h4= property.address
      %dl
        %dt Rooms:
        %dd= property.bedrooms
        %dt Parking:
        %dd= property.parking
        %dt Rent:
        %dd
          $#{property.price}
          %span.pay_period
            \/#{property.unit}
  - if index % 3 == 2
    </div>